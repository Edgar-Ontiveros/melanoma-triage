# F4 — Evaluación clínica

Especificación para Claude Code. Guardar como `docs/specs/F4.md`.

**Duración estimada:** 1–2 semanas. Sin GPU: todo es inferencia en la laptop.
**Depende de:** F3 cerrada, DDI en `data/raw/ddi/` y fuera de git.
**Bloquea:** F5 en adelante y el capítulo de resultados de la tesis.

---

## Objetivo

Producir el número que se defiende. Se abre el conjunto de prueba **una sola vez**, con
todas las decisiones tomadas y congeladas de antemano, y se reporta lo que sale. Después,
evaluación externa en DDI para medir la degradación fuera del dominio y por tono de piel.

**La regla que gobierna la fase:** toda decisión —calibración, umbral, métricas, subgrupos,
diseño de DDI— se toma sobre validación y se escribe en un protocolo antes de tocar el test.
Una decisión tomada después de ver el test contamina el test.

---

## Contexto fijado

| Aspecto | Valor |
|---|---|
| Modelo | A1: `tf_efficientnetv2_s.in21k_ft_in1k` a 224 px, semilla 0 |
| Checkpoint | `melanoma-f3-final` en Kaggle, SHA256 `e0ecf981…4710040` |
| Validación | 4,963 imágenes, 88 melanomas, 568 pacientes |
| **Test** | 5,252 imágenes, 86 melanomas, 540 pacientes, **intacto** |
| DDI | 656 imágenes, 171 malignas, sin id de paciente |
| Punto de operación | Sensibilidad ≥ 0.95 (triage) |
| Presupuesto de accesos al test | 2 declarados; **esta fase usa 1** |

**Nada de esta fase cambia el modelo.** Ni ensamble de semillas, ni fusión con metadatos, ni
TTA. El modelo evaluado es exactamente el que F3 declaró. Cualquier mejora al modelo sería
otra decisión sobre validación y va a trabajo futuro.

---

## Una advertencia que va en el reporte

Validación ya está gastada. Sobre ella se eligieron: la línea base, el desbalance, la
arquitectura, la resolución, la época de cada corrida, y ahora la calibración y el umbral.
Sus métricas están sesgadas al alza por toda esa selección.

**El test es la única estimación limpia.** Si el test sale por debajo de validación, eso no
es un bug: es la brecha de generalización, y es informativa. Se reporta la diferencia.

---

## F4.0 — Preparación local

1. Bajar el checkpoint de `melanoma-f3-final` a `data/models/` (fuera de git; verificar
   `.gitignore`). Comprobar el SHA256 contra `reports/f3_final_model.json`. **Si no coincide,
   detenerse.**
2. Corregir la plantilla de nombres de `ModelCheckpoint` (el sufijo `0.0000` de F3). Sin
   reentrenar; solo para corridas futuras.
3. Generar las predicciones sobre **validación** con el checkpoint, en CPU, guardando
   `logit`, `prob_raw`, `image_id`, `patient_id`, `target` y los metadatos. Son ~4,963
   imágenes a 224 px: minutos en la laptop.

---

## F4.1 — Calibración sobre validación

### Por qué Platt y no solo temperature scaling

`pos_weight = 55` entrena el modelo como si los positivos fueran 55 veces más frecuentes.
Eso desplaza todos los logits hacia arriba por aproximadamente `ln(55) ≈ 4.0`, además de la
sobreconfianza habitual de una red neuronal.

- **Temperature scaling** (`z / T`) corrige la escala, no el desplazamiento.
- **Platt scaling** (`a·z + b`) corrige las dos cosas.

Se ajustan las dos sobre validación y se reportan; **Platt es la primaria**. Verificación
teórica que va en el reporte: `b` debería salir cerca de `−ln(55)`. Si no, explicar por qué.

Ambas preservan el orden: AUC-ROC y AUPRC no cambian. Se verifica con una prueba.

### Métricas de calibración

Antes y después, sobre validación:

| Métrica | Qué mide |
|---|---|
| ECE (10 bins, ponderado) | Distancia promedio entre probabilidad predicha y frecuencia observada |
| Brier | Error cuadrático de la probabilidad |
| Diagrama de fiabilidad | La figura |

**Cuidado con los bins vacíos.** Con 1.77 % de prevalencia, casi todo cae en el primer bin.
Usar bins por cuantiles además de los uniformes, y reportar cuántas muestras tiene cada uno.

---

## F4.2 — Selección del umbral sobre validación

Con las probabilidades **calibradas**:

1. **Umbral primario `τ₉₅`:** el menor umbral tal que sensibilidad ≥ 0.95.
2. **Umbral secundario `τ₉₀`:** lo mismo a 0.90, para continuidad con F3.

### Por bootstrap, no por un corte

Con 88 melanomas, sensibilidad ≥ 0.95 significa atrapar 84 o más. El umbral de un solo corte
depende de cuatro casos individuales. En su lugar:

- 2,000 remuestreos por paciente de validación
- En cada uno, calcular el umbral que da sensibilidad ≥ 0.95
- **`τ₉₅` = la mediana** de esa distribución; reportar también percentiles 5 y 95

Reportar la especificidad, VPP y VPN que ese umbral produce sobre validación completa.

---

## F4.3 — Congelar el protocolo

**Este es el paso que hace defendible la fase.** Antes de que exista siquiera un script que
lea el test, se escribe `docs/f4_protocol.yaml` con:

```
model:
  checkpoint_sha256: ...
  git_sha: ...
calibration:
  method: platt
  a: ...
  b: ...
  fitted_on: val
thresholds:
  tau_95: ...
  tau_90: ...
  method: bootstrap-median, 2000 resamples, patient-level
metrics: [auroc, auprc, sens_at_spec_090, sens_at_spec_095, spec_at_sens_090,
          spec_at_sens_095, ece, brier, confusion_at_tau_95, confusion_at_tau_90]
bootstrap: {n: 2000, unit: patient, seed: ...}
subgroups:
  age: [<40, 40-59, 60+, unknown]
  sex: [male, female, unknown]
  site: [as in manifest, unknown]
ddi:
  primary_task: malignant_vs_benign
  secondary_task: melanoma_vs_rest
  groups: [fst_12, fst_34, fst_56]
  bootstrap_unit: image   # DDI no tiene id de paciente
```

Se commitea. **El commit del protocolo es anterior al commit del script de evaluación del
test.** El historial de git lo demuestra.

### Prueba estructural

`test_protocol_committed`: `scripts/evaluate_test.py` se niega a correr si
`docs/f4_protocol.yaml` tiene cambios sin commitear. Así el protocolo que se usó es
verificablemente el que está en git.

---

## F4.4 — El acceso único al test

`scripts/evaluate_test.py`. Único archivo del proyecto autorizado a leer `test.txt`
(`test_test_split_isolation` lo vigila desde F1).

**Orden obligatorio dentro del script:**

1. Verificar que el protocolo esté commiteado (falla si no).
2. Verificar el SHA256 del checkpoint contra el protocolo.
3. **Escribir en `logs/test_set_access.log`** — timestamp, git SHA, hash del protocolo,
   propósito — **antes de leer una sola imagen.** El registro es incondicional: si el script
   falla a la mitad, el acceso ya quedó registrado.
4. Leer `test.txt`, cargar imágenes, inferir en CPU.
5. Aplicar calibración y umbrales **tal como están en el protocolo**. Nada se reajusta.
6. Calcular todas las métricas con bootstrap por paciente.
7. Escribir `reports/f4_test_results.json` y las figuras.

### Prueba estructural

`test_access_logged_before_read`: con el sistema de archivos simulado, el script escribe en
la bitácora antes de abrir `test.txt`. Si alguien reordena el código, la prueba falla.

**Después de este paso, el test no se vuelve a tocar en F4.** Si algo salió mal en el script
—un bug, un archivo corrupto— se corrige y se usa el segundo acceso, y queda registrado como
segundo acceso con su motivo. No se "reintenta en silencio".

---

## F4.5 — Análisis por subgrupos sobre el test

Con las mismas predicciones de F4.4, sin volver a inferir:

- **Edad:** <40, 40–59, 60+, desconocida
- **Sexo:** masculino, femenino, desconocido
- **Sitio anatómico:** las categorías del manifiesto, más desconocido

Para cada subgrupo: n, positivos, AUC-ROC con IC, sensibilidad y especificidad en `τ₉₅`.

**Advertencia obligatoria en el reporte:** con 86 melanomas en test, los subgrupos tienen
entre 5 y 30 positivos cada uno. Los intervalos van a ser anchos. Este análisis es
**exploratorio**: detecta diferencias grandes, no confirma diferencias pequeñas. No se
concluye nada de una diferencia cuyo intervalo cruce el del grupo de referencia.

---

## F4.6 — Evaluación externa en DDI

**Corre local, en CPU. DDI no sale de la laptop.**

### Dos cambios a la vez, y se dicen

DDI difiere de ISIC en dos dimensiones:

| Dimensión | ISIC 2020 | DDI |
|---|---|---|
| Tipo de imagen | Dermatoscopía | Foto clínica |
| Etiqueta | Melanoma vs resto | Maligno vs benigno (78 diagnósticos) |
| Prevalencia | 1.76 % | 26 % |
| Encuadre | Lesión centrada, fondo uniforme | Variable |

El modelo va a bajar. Mucho. Eso es lo esperado y es el hallazgo: cuantifica lo que tu v1
ignoró cuando probó con una foto de celular.

### Diseño

**Tarea primaria: maligno vs benigno.** Es la etiqueta de DDI, la que usó Daneshjou et al.,
y la que da positivos suficientes por grupo de tono (49 / 74 / 48). **Es un cambio de tarea**
respecto al entrenamiento y se declara así en el reporte.

**Tarea secundaria: melanoma vs resto.** Coincide con la tarea del modelo. Contar primero
cuántos melanomas hay en la columna `disease`; probablemente son pocos y los intervalos
serán enormes. Se reporta igual, como comprobación.

**Por grupo Fitzpatrick** (I–II, III–IV, V–VI): AUC-ROC con IC por bootstrap **a nivel
imagen** (DDI no tiene id de paciente; se declara la limitación).

**Preprocesamiento:** la misma transformación de validación (lado corto + CenterCrop 224).
Las fotos clínicas no tienen la lesión centrada como la dermatoscopía; el recorte central
puede cortarla. Se reporta como fuente adicional de degradación, no se corrige — corregirlo
sería adaptar el modelo a DDI.

**Calibración en DDI:** aplicar la de ISIC y reportar el ECE. Va a salir mal: la prevalencia
es 15 veces distinta. Es un resultado, no un error: la calibración no se transfiere entre
dominios ni prevalencias.

### Anclaje

El paper de DDI reporta que los modelos del estado del arte cayeron a AUC 0.56–0.67 sobre
DDI, con peor desempeño en piel oscura. Citar con verificación en fuente
(Daneshjou et al., *Science Advances* 2022, DOI 10.1126/sciadv.abq6147) y situar tu
resultado contra el suyo.

---

## F4.7 — Reporte

**Entregable: `reports/f4_clinical_evaluation.md`**, generado por script, con estas
secciones en este orden:

1. **Protocolo** — copia del YAML congelado y el SHA del commit donde se congeló.
2. **Bitácora** — la línea de `test_set_access.log` de esta evaluación.
3. **Calibración** — antes/después sobre validación; `a`, `b`, `T`; comparación de `b` con
   `−ln(55)`; diagramas de fiabilidad.
4. **Resultados sobre el test** — tabla completa con IC por paciente. AUC-ROC, AUPRC,
   sensibilidad/especificidad a niveles fijos, ECE y Brier con la calibración congelada.
5. **Punto de operación** — matriz de confusión en `τ₉₅` y `τ₉₀`; sensibilidad,
   especificidad, VPP y VPN a la prevalencia observada; y **proyección del VPP y VPN a
   prevalencias de 1 %, 2 % y 5 %** (aritmética directa desde sensibilidad y especificidad),
   para traducir el modelo a distintos entornos clínicos.
6. **Validación vs test** — la brecha, con la advertencia de selección.
7. **Subgrupos** — tabla con la advertencia de exploratorio.
8. **DDI** — las dos tareas, los tres grupos de tono, calibración, anclaje en el paper.
9. **Comparación con la v1** — la tabla que cierra el círculo:

| | v1 (2024) | v2 (este trabajo) |
|---|---|---|
| Conjunto reportado | Validación, contaminada | Test bloqueado, un acceso |
| Prevalencia | 48 % | 1.64 % |
| Métrica principal | Accuracy 90.39 % | AUC-ROC y AUPRC con IC |
| Umbral | 0.5 por omisión | `τ₉₅` por bootstrap sobre validación |
| Calibración | Ninguna | Platt sobre validación |
| Fugas verificadas | No | Sí, por paciente |
| Sesgo por tono de piel | No evaluado | DDI, tres grupos |
| Dominio fuera de dermatoscopía | Una foto sin diagnóstico | 656 imágenes con biopsia |

10. **Limitaciones** — escritas por ti, no generadas: lo que estos números no dicen.

**Opcional si sobra tiempo:** curva de decisión (net benefit) sobre el test. Es el estándar
clínico para mostrar utilidad y casi nadie lo hace en una tesis de licenciatura.

---

## Pruebas a agregar

| Prueba | Qué verifica |
|---|---|
| `test_calibration_preserves_ranking` | AUC-ROC y AUPRC idénticos antes y después de Platt y de temperature |
| `test_platt_recovers_known_shift` | Con logits sintéticos desplazados por +4, Platt recupera `b ≈ −4` |
| `test_protocol_committed` | `evaluate_test.py` se niega a correr con el protocolo sin commitear |
| `test_access_logged_before_read` | La bitácora se escribe antes de abrir `test.txt` |
| `test_ddi_never_uploaded` | Ningún script bajo `scripts/` ni `notebooks/` referencia rutas de DDI junto a llamadas de Kaggle |

---

## Criterios de aceptación

1. Checkpoint descargado, SHA256 verificado.
2. Calibración ajustada sobre validación; Platt y temperature reportadas; `b` comparado con
   `−ln(55)`; ECE y Brier antes/después.
3. `τ₉₅` y `τ₉₀` por bootstrap, con su distribución.
4. `docs/f4_protocol.yaml` commiteado **antes** del script de evaluación; el historial lo
   muestra.
5. `logs/test_set_access.log` tiene exactamente **una** línea, con timestamp, git SHA y hash
   del protocolo.
6. `reports/f4_test_results.json` y las figuras, sin ningún reajuste posterior.
7. Subgrupos con IC y advertencia de exploratorio.
8. DDI evaluado localmente, dos tareas, tres grupos, calibración, anclaje verificado.
9. `reports/f4_clinical_evaluation.md` con las diez secciones.
10. Las cinco pruebas nuevas en verde; `make ci`; push. DDI y el checkpoint fuera de git.

---

## Fuera de alcance

- **Cambiar el modelo.** Ensamble, fusión de metadatos, TTA, reentrenar con otra época:
  todo eso es una decisión nueva sobre validación y no entra en esta fase.
- **Reajustar nada después de ver el test.** Si los números decepcionan, se reportan.
- **Grad-CAM.** F5.
- **Optimizar el preprocesamiento para DDI.** Sería adaptar el modelo al conjunto de prueba
  externo.
- **Segundo acceso al test.** Queda en reserva. Solo se usa si F4.4 falló por un error de
  código, y se registra con su motivo.

---

## Orden de trabajo

**Sesión 1:** F4.0 y F4.1. Checkpoint, predicciones sobre validación, calibración.
**Sesión 2:** F4.2 y F4.3. Umbral por bootstrap, escribir y commitear el protocolo.
**Sesión 3:** F4.4 y F4.5. **Un solo acceso.** Test y subgrupos.
**Sesión 4:** F4.6. DDI.
**Sesión 5:** F4.7. Reporte, y tú escribes las limitaciones.

Entre la sesión 2 y la 3 conviene una pausa: revisar el protocolo con calma antes de
ejecutarlo. Una vez abierto el test, no hay vuelta atrás.

---

## Qué esperar

Sobre el test, algo cercano a validación: **AUC-ROC 0.80–0.85, AUPRC 0.10–0.17**. Una caída
de hasta 0.03 respecto a validación es la brecha de selección y es normal.

A sensibilidad 0.95, la especificidad probablemente quede entre **0.25 y 0.40**. Eso
significa que, para atrapar 95 de cada 100 melanomas, el sistema refiere entre 60 y 75 de
cada 100 pacientes sanos. Es un número duro, y es el número honesto de un modelo único con
GPU gratuita. La tesis lo dice tal cual y explica qué haría falta para mejorarlo.

Sobre DDI, **AUC 0.55–0.70** en maligno/benigno, probablemente peor en piel oscura. Si tu
modelo se degrada como los del paper, eso confirma su hallazgo con un modelo independiente.
Si no se degrada por tono, también es un resultado, y hay que explicarlo con cuidado.

**Lo que no se hace:** volver a validación a "arreglar" algo después de ver el test.

---

## Estado y desviaciones respecto a la spec

Sesiones 1, 2 y 4 hechas el 2026-09-18; **sesión 3 (el acceso único al test) en pausa
deliberada hasta que Edgar revise el protocolo**, como recomienda la spec.

1. **F4.0.** Checkpoint bajado a `data/models/` (fuera de git, entrada explícita en
   `.gitignore`); SHA256 idéntico al de `reports/f3_final_model.json`. Plantilla de
   `ModelCheckpoint` corregida (`auprc{val/auprc:.4f}`, con prueba); el archivo de F3 conserva
   su nombre. Predicciones de validación en CPU: 4,963 imágenes en 4 min
   (`reports/predictions/f4_val.csv`, con logit, probabilidad cruda y metadatos).
2. **F4.1.** Platt a = 0.485, b = −3.807 frente a −ln(54.88) = −4.005 (diferencia +0.20: el
   modelo estaba menos desplazado de lo que predice `pos_weight` puro, porque la pendiente
   a < 1 absorbe parte del sesgo). Temperatura T = 1.83. Brier 0.154 → 0.016 (Platt) y
   0.142 (temperatura); ECE uniforme 0.226 → 0.002 / 0.269. Orden preservado (prueba).
   `pos_weight_used` se toma de `metrics.json` de a1-s0 (54.88).
3. **F4.2.** τ95 = 0.0039 (p5 0.0013, p95 0.0063; corte único 0.0039) → sobre validación
   sensibilidad 0.955, especificidad 0.400, VPP 0.028, VPN 0.998. τ90 = 0.0066 (p5 0.0040,
   p95 0.0112) → 0.898 / 0.537. Umbrales pequeños porque son probabilidades calibradas a
   prevalencia 1.77 %.
4. **F4.3.** `docs/f4_protocol.yaml` commiteado solo, en `23d817a`, antes de que existiera
   `scripts/evaluate_test.py`; `test_protocol_is_committed_in_this_repo` compara las fechas
   de los commits que añadieron cada archivo.
5. **F4.4 escrito, no ejecutado.** `scripts/evaluate_test.py` sigue el orden obligatorio;
   `test_access_logged_before_read` lo verifica con el sistema de archivos simulado y otra
   prueba comprueba el orden en el texto del script. `logs/test_set_access.log` sigue vacío.
6. **F4.6 corrida (antes que F4.4: no toca el test).** Maligno vs benigno AUC-ROC 0.596
   [0.541, 0.648]; por tono 0.590 / 0.596 / 0.604 con intervalos traslapados: sin la caída en
   piel oscura del paper. Melanoma vs resto: 21 positivos (7 por grupo), 0.532 [0.408, 0.657].
   Calibración de ISIC en DDI: ECE 0.229, Brier 0.242; τ95 de ISIC refiere el 99 %. Cita de
   Daneshjou et al. verificada en el texto completo (PMC9374341): 0.56–0.67 en los tres
   algoritmos del estado del arte. Las predicciones sobre DDI quedan en `data/raw/ddi/`, fuera
   de git. `test_ddi_never_uploaded` vigila que ningún script mezcle DDI con Kaggle.
7. **Reporte** (`scripts/f4_report.py`) con las diez secciones; 2, 4, 5, 6 y 7 pendientes hasta
   la sesión 3, y la 10 se incluye desde `docs/f4_limitations.md` cuando Edgar la escriba.
